add_custom_target(
    LIBS_UPDATE
    COMMAND pio pkg update
    WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)

add_custom_target(
    CLEAN_ALL
    COMMAND platformio.exe run --target clean
    WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)

add_custom_target(
    BUILD_ALL
    COMMAND platformio.exe run
    WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)

add_custom_target(
    UPLOAD_ALL
    COMMAND platformio.exe run --target upload
    WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)

add_custom_target(
    ERASE_ALL_ESP32
    COMMAND platformio.exe run --target erase --environment glowwormluciferinfull_esp32
    WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)

add_custom_target(
    ERASE_ALL_ESP8266
    COMMAND platformio.exe run --target erase --environment glowwormluciferinfull_esp8266
    WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)

add_custom_target(
    UPLOAD_FS_ALL
    COMMAND platformio.exe run --target uploadfs
    WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)

